using UnityEngine;

public class HelloWorld : MonoBehaviour // class with a modifier "MonoBehaviour", which allow the script to be attached to a game object
{
    private void Start() // initialization method, called when the object is first 
    {
        Debug.Log("Hello World");
    }
}
